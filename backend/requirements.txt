# Web 框架
fastapi>=0.115
uvicorn[standard]>=0.30

# 数据层
sqlalchemy>=2.0
psycopg[binary]>=3.1
pgvector>=0.2

# 配置与校验
pydantic>=2.7
pydantic-settings>=2.3

# 认证
PyJWT>=2.8
bcrypt>=4.0

# AI 适配层(兼容 openai / deepseek / 通义等 OpenAI 风格 SDK)
openai>=1.30
httpx>=0.27

# 表单与上传
python-multipart>=0.0.9

# 测试
pytest>=8.0
pytest-asyncio>=0.23
